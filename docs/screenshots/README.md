# Capturas del README

Las capturas se toman con los fixtures sintéticos de `docs/demo-data/`. Nunca
uses el perfil real: muestran cuotas, historial, saldo DeepSeek y recargas de
ejemplo sin credenciales.

En macOS, seguí el procedimiento de `.agents/HANDOFF-codex-next.md`. Guardá
las imágenes finales en esta carpeta como `macos-app.png`,
`macos-desktop-widget.png` y `macos-menu-bar.png`.

Las capturas GNOME equivalentes se nombran `linux-app.png`,
`linux-desktop-widget.png` y `linux-panel-menu.png`.
